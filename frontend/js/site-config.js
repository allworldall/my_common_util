/**
 * 站点配置（SEO / sitemap 用）
 *
 * 上线请在 deploy.env 里设置 SITE_ORIGIN（如 https://tools.example.com），
 * 发布脚本会把下面的占位符替换成真实域名。
 * 未替换时，页面脚本会回退到当前浏览器地址。
 */
window.SITE_CONFIG = {
  origin: "__SITE_ORIGIN__",
  siteName: "ToolKit",
  siteNameFull: "ToolKit 在线工具集",
};
