# Test：PDF 合并

对照 spec 验收标准逐条勾选。

## 自测

- [x] 首页文件处理分类能看到「PDF合并」，排在拆分和压缩之间，描述为将两个 PDF 按顺序合成一个
- [x] A（2 页）在第 1 槽、B（3 页）在第 2 槽合并后共 5 页，顺序 A1、A2、B1、B2、B3
- [x] 先点「交换顺序」再合并，顺序为 B 全部页再接 A 全部页（接口按对调后的 file1/file2 验证；前端交换只对调本地槽、不再请求 info）
- [x] 只选了一个文件时，「合并并下载」不可点（按钮默认 disabled，两槽齐了才启用）
- [x] 非 PDF 或改后缀伪装：槽位拒绝并提示，不发合并请求
- [x] 单文件超过 50MB：提示「文件大小不能超过50M」
- [x] 两文件合计页数超过 200：合并失败并提示页数上限
- [x] 加密 PDF：读信息或合并失败，提示「不支持加密的 PDF」
- [x] 合并成功弹出保存框，可选「选择位置保存」或「浏览器下载」，打开结果正常（弹窗与拆分对齐；本机用接口下载打开结果）
- [x] 不同纸张尺寸的两个 PDF 合并后，各页尺寸与原文件一致（A4 595×842 + A5 420×595）
- [x] 处理完成后服务端无长期保留的上传/结果文件
- [x] 下载名是 `{名1}+{名2}.pdf`；超长主名截到整名 ≤180 字符
- [x] FAQ 写明：只保证页面按顺序拼在一起；加密打不开；可填写表格 / 左侧目录 / 电子章可能没了

## 联调

- [x] 环境与数据准备：本机 `SPRING_PROFILES_ACTIVE=local` 起后端，前端用现有本地静态方式打开合并页；准备上述样例 PDF
- [x] 上下游：`/api/pdf/info` 两次 + `/api/pdf/merge` 一次；带 `X-Api-Token` 的环境请求头与拆分一致（本机 token 为空，与拆分相同）
- [ ] 发版时按 SOP 改线上 nginx（加 `/api/pdf/merge` 110m，`nginx -t && reload`）后，两个合计 >52MB 的文件能合并，而不是网关 413

## 回归

- [x] PDF 拆分：上传、读页数、按页拆分、保存弹窗仍正常
- [x] PDF 压缩：上传与三档压缩仍正常（接口仍在；本机无 Ghostscript，返回 503 提示安装，属原有依赖，不是合并改坏的）
- [x] 图片扫描 / Word↔PDF：入口和原接口未坏（页面 200，接口仍映射）
- [x] 首页其它分类工具链接仍可点
